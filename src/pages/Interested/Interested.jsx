import React, { Component } from 'react';

class Interested extends Component {

    render() {
      return (
        <header className="App-header"> <h1> Interested </h1> </header> 
      );
    }
  }
  
  export default Interested;